# Python program to add all elements in a list using a for loop

# Define a list
numbers = [1, 2, 3, 4, 5]

# Initialize a variable to store the sum
total = 0

# Loop through each element and add to total
for num in numbers:
    total += num

# Print the result
print("Sum of all elements:",total)